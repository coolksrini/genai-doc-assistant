[pytest]
asyncio_mode = auto
markers =
    unit: fast, no external dependencies
    integration: requires FastAPI test client
    e2e: requires Ollama running + sample datasets
    regression: full regression over all formats and edge cases
filterwarnings =
    ignore::DeprecationWarning
    ignore::UserWarning
